public class Student {
    private String name;
    private int rollNumber;

    // Default constructor
    public Student() {
        this.name = "";
        this.rollNumber = 0;
    }

    // Method to set the name
    public void setName(String name) {
        this.name = name;
    }

    // Method to get the name
    public String getName() {
        return this.name;
    }

    // Method to set the roll number
    public void setRollNumber(int rollNumber) {
        this.rollNumber = rollNumber;
    }

    // Method to get the roll number
    public int getRollNumber() {
        return this.rollNumber;
    }

    public static void main(String[] args) {
        // Instantiate an object of the Student class using the default constructor
        Student studentObj = new Student();

        // Set the name to "Sarah" and roll number to 102
        studentObj.setName("Sarah");
        studentObj.setRollNumber(102);

        // Access the methods to get the attributes
        String name = studentObj.getName();
        int rollNumber = studentObj.getRollNumber();

        // Print the attributes
        System.out.println("Name: " + name);
        System.out.println("Roll Number: " + rollNumber);
    }
}
